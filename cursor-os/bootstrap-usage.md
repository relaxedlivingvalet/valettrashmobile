# Bootstrap Usage

## Node
node scripts/init-cursor-os.js

## With force
node scripts/init-cursor-os.js --force

This script creates missing Repo OS files and skips existing ones unless --force is used.
